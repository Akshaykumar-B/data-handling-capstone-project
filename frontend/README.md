# Dashboard frontend

Next.js App Router scaffold for the public transit dashboard. Analytics pages
are intentionally placeholders until the FastAPI data contract is expanded.

Set `NEXT_PUBLIC_API_BASE_URL` to the FastAPI origin before running locally.